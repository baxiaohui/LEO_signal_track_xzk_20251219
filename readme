# LEO 跟踪与解调仿真代码（BPSK + CSK 时分结构）

本仓库包含一套用于 **LEO 导航增强信号**（BPSK 与 CSK 分量时分复用）的接收机**跟踪与解调**仿真代码。代码支持多种跟踪策略（PLL/FLL/KF/EKF/UKF 等），并可输出跟踪误差统计、CSK 解调 BER 统计以及批量仿真结果表格，主要用于论文复现实验与对比评估。

> 说明：本代码以科研复现为目标，默认参数以论文仿真设置为准；如需工程化/实时化，请根据平台与接口进一步重构。

---

## 1. 功能概览

### 1.1 支持的跟踪/解调模式（mode）
代码通过 `mode` 选择不同算法分支（示例含义如下，具体以代码注释为准）：

- `mode = 0`：PLL（锁相环）
- `mode = 1`：FLL（锁频环，含 HDC-FLL 相关逻辑）
- `mode = 2`：LKF（线性卡尔曼滤波）
- `mode = 3`：EKF（扩展卡尔曼滤波，剥离电文）
- `mode = 4`：EKF（不剥离电文，**当前版本标注为暂不可用/需推导完善**）
- `mode = 5`：CSK 解调专用（不做跟踪调整，偏向观测/统计）
- `mode = 6`：UKF（无迹卡尔曼滤波，剥离电文）
- `mode = 8`：LKF 外推/调度版本（示例：1 ms 预测 + 2 ms 矫正）
- `mode = 9`：EKF 外推/调度版本（示例：1 ms 预测 + 2 ms 矫正，剥离电文）

### 1.2 统计与输出
- 跟踪误差：载波相位误差、Doppler 误差、C/N0 估计等
- 解调误码：CSK 相干/非相干 BER，及（可选）LDPC 纠错后的 BER
- 结果落盘：日志 txt、结果 Excel、以及每次仿真的 workspace `.mat` 快照

---

## 2. 运行环境

- MATLAB：建议 R2021a 或更高版本
- 操作系统：Windows 环境更省事（涉及 `xlswrite/xlsread` 写 Excel）
  - 若在 Linux/macOS 上运行：可改用 `writetable` / `writecell` 等方式替代 `xlswrite`
- 可能用到的工具箱（视你代码调用而定）：Signal Processing / Communications（若未使用可忽略）

---

## 3. 代码与数据依赖（请务必放到 MATLAB path）

### 3.1 必需文件/函数（示例）
- `CAgenerate.m`：生成本地码（低速/高速）
- `BIThightrans.m`：高速电文比特预处理/映射
- `EMSdecoderforcsk.m`：CSK 的纠错/译码（如 EMS 译码）
- `countBERandSER.m`：统计 BER/SER
- `Trackplotandcount.m`：统计跟踪误差并绘图/输出统计量

### 3.2 必需数据文件（示例）
- `LDPC200_100high.mat`：包含 LDPC 编码后的比特序列（如变量 `resultbin1`）

---

## 4. 快速开始（最小复现）

1. 将主脚本保存为 `main.m`（或你自己的文件名），并确保第 3 节列出的函数/数据文件均在 MATLAB 搜索路径中。
2. 在脚本中设置关键参数（见下一节），例如：
   - `CN0`（dB-Hz）
   - `mode`
   - `AcqSatNUM`（PRN）
   - `LocalDop`（初始多普勒）
3. 运行：
   - MATLAB 命令行执行 `main`，或直接点击运行按钮。
4. 运行结束后查看输出文件（第 6 节）。

---

## 5. 关键参数说明（建议从这里改）

- `CN0`：设定载噪比（dB-Hz），影响信号幅度、解调开关与统计表现  
- `mode`：选择算法模式（见 1.1）
- `AcqSatNUM`：待跟踪卫星 PRN 编号
- `LocalDop`：初始多普勒（建议与真实/设定值接近）
- `Freq_sample`：采样频率（Hz）
- `Fc`：中频载波（Hz）
- `Freq_code`：码率（Hz）
- `runtime`：程序运行时间（秒），部分版本会根据可视弧段自动计算
- `anger`：几何可视范围相关角度参数（用于构造过境时长/多普勒序列）

---

## 6. 输出结果（运行后会生成）

- `LKF.txt`（示例名）：运行日志（每次更新记录 loopcnt、载波频率、鉴相/创新等）
- `simulation_results.xlsx`：仿真汇总表（自动追加写入）
  - 列一般包括：载噪比、mode、BER（相干/非相干/LDPC 相干）、Carrier Phase RMSE、Doppler RMSE、C/N0 Mean 等
- `workspace_data_<CN0>_<mode>.mat`：保存当次运行的工作区变量快照，便于复现实验与画图

---

## 7. 复现实验建议（用于论文对比）

- 建议按论文设置固定一组：`mode` × `CN0` 扫描
- 每个组合运行结束后：
  1) 读取 `simulation_results.xlsx` 汇总画图  
  2) 如需复盘细节，加载对应的 `workspace_data_<CN0>_<mode>.mat`  
- 为了可重复性：
  - 保持随机种子固定（如 `rng(1)`）
  - 统一参数（环路带宽、相干积分时间、非相干累加次数等）

---

## 8. 常见问题（FAQ）

**Q1：运行报找不到 `LDPC200_100high` 或 `resultbin1`？**  
A：请确认 `LDPC200_100high.mat` 在当前目录或 MATLAB path 中，并包含变量 `resultbin1`。

**Q2：`xlswrite/xlsread` 在非 Windows 环境不可用或报错？**  
A：建议替换为 `writecell/writetable`，或在 Windows 下运行生成 Excel，再在其他系统做后处理。

**Q3：mode=4 提示不可用/效果异常？**  
A：该分支属于“不剥离电文的 EKF（含电文状态）”试验版，需进一步推导与完善后再用于论文结论。

---

## 9. 引用与声明

如你在论文/报告中使用了本仓库代码，请在致谢或代码可用性声明中注明来源（作者/仓库链接/版本号或 commit hash）。

- 作者：许哲锴
- 用途：科研复现与算法对比

---

## 10. 联系方式

如需复现实验的参数对齐、输出指标解释或新增对比脚本，可在仓库 Issues 中留言或联系作者。
